name: docker

# 构建多架构镜像并推送到 GHCR：
#   ghcr.io/<owner>/proxy-checker:<tag>      （推送 v* tag 时，例如 v6.5）
#   ghcr.io/<owner>/proxy-checker:latest     （打 tag 或手动触发时，指向最近一次构建）
#   ghcr.io/<owner>/proxy-checker:sha-xxxxxxx
#
# 触发方式：只由 v* tag 推送或手动 workflow_dispatch 触发，不监听分支推送。
# 这样一次发布只产生一个 run，latest 与 sha-* 不会被多个 run 争写。
#
# 首次推送后，需要在 GitHub 仓库的 Packages 页面把镜像设为 Public，
# 否则服务器上拉取镜像需要先 docker login ghcr.io。

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=tag
            type=sha,format=short

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
